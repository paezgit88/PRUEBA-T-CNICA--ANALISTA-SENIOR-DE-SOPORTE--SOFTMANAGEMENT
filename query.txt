


/*inserts*/
/*insert ciudad*/
INSERT INTO ciudad (COD_CIUDAD,CODIGO_DEPTO, DESCRIPCION_CIUDAD) VALUES (NULL,2,'descripcion'); 

/*insert clientes*/
INSERT INTO clientes (ID_CLIENTE,TIPO_IDENTIFICACION,NUMERO_IDENTIFICACION,NOMBRES,APELLIDOS,CARGO,MAIL,DIRECCION,TELEFONO,CODIGO_CIUDAD,CODIGO_USUARIO) VALUES (NULL, 'CEDULA DE CIUDADANIA', '123456789', 'USUARIO TEST', 'NUMERO UNO', 'VENDEDOR', 'CORREO@CORREO.COM', 'DIRECCION 12-34 5', '123456789654',1,1); 

/*insert departamento*/
INSERT INTO departamento (CODIGO_DEPTO,DESCRIPCION_DPTO) VALUES (NULL, 'DEPARTAMENTO TEST'); 

/*insert productos*/
INSERT INTO productos (CODIGO_PRODUCTO,DESC_PRODUCTO) VALUES (NULL, 'PRODUCTO TEST'); 

/*insert usuarios*/
INSERT INTO usuarios (CODIGO_USUARIO, USUARIO,CLAVE) VALUES (NULL, 'USUARIO TEST', 'CLAVE_DEL_USUARIO_123'); 

/*insert ventas*/
INSERT INTO ventas (ID_CLIENTE, COD_CIUDAD, CODIGO_PRODUCTO, FECHA_VENTA,VALOR_VENTA, CANTIDAD_PRODUCTO) VALUES (NULL,1,1, '2022-12-11 14:12:33', 250000, 1); 







/*selects*/

SELECT c.ID_CLIENTE, c.NOMBRES,c.APELLIDOS,c.DIRECCION,ct.DESCRIPCION_CIUDAD,u.USUARIO FROM tienda.CLIENTES c, tienda.CIUDAD ct, tienda.USUARIOS u where c.NUMERO_IDENTIFICACION='123456789' AND c.TIPO_IDENTIFICACION='CEDULA DE CIUDADANIA' AND c.CODIGO_CIUDAD=ct.COD_CIUDAD AND c.CODIGO_USUARIO=u.CODIGO_USUARIO 



SELECT c.ID_CLIENTE, c.NOMBRES,c.APELLIDOS,v.FECHA_VENTA,p.DESC_PRODUCTO,v.VALOR_VENTA FROM tienda.CLIENTES c, tienda.PRODUCTOS p, tienda.VENTAS v where v.FECHA_VENTA >'01/10/2010 00:00:00' AND v.FECHA_VENTA < '31/12/2010 23:00:00' AND v.CODIGO_PRODUCTO=p.CODIGO_PRODUCTO 


/*alter table*/

ALTER TABLE tiendas.CLIENTES ADD COLUMN CLASE VARCHAR (20);


/*updates*/

((SELECT SUM(v.VALOR_VENTA) AS TOTAL_VENTAS, v.ID_CLIENTE FROM tienda.VENTAS v WHERE VALOR_VENTA >= 200000 GROUP by v.ID_CLIENTE)) 

((SELECT SUM(v.VALOR_VENTA) AS TOTAL_VENTAS, v.ID_CLIENTE FROM tienda.VENTAS v WHERE VALOR_VENTA >= 100000 GROUP by v.ID_CLIENTE)) 

((SELECT SUM(v.VALOR_VENTA) AS TOTAL_VENTAS, v.ID_CLIENTE FROM tienda.VENTAS v WHERE VALOR_VENTA < 100000 GROUP by v.ID_CLIENTE)) 

UPDATE tienda.CLIENTES c SET c.CLASE ='ALTO' WHERE c.ID_CLIENTE IN (1,2) 


/*deletes*/

DELETE FROM tienda.VENTAS WHERE ID_CLIENTE IN(SELECT c.ID_CLIENTE FROM tienda.CLIENTES c WHERE c.CLASE='BAJO');

DELETE FROM tienda.USUARIOS WHERE CODIGO_USUARIO IN(SELECT c.CODIGO_USUARIO FROM tienda.CLIENTES c WHERE c.CLASE='BAJO');

DELETE FROM tienda.CLIENTES WHERE CLASE='BAJO' ;